# 복기

중요했던 건 DFS냐 BFS냐가 아니라

중복 탐색을 최소화하기 위해

석유 덩어리(connected component)의 크기를 따로 계산해두는 설계였다.

DFS/BFS는 그 덩어리를 한 번 순회하기 위한 수단일 뿐이며,

큐를 쓰는 BFS든 재귀 DFS든 논리적으로는 상관없다.

---

* DFS로 중복 탐색 → 시간초과

* 덩어리 메타데이터 분리 → 탐색 1회

* DFS/BFS는 전략 차이지 정답 차이 아님